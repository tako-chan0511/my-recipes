version: '3.8'

services:
  backend:
    build: ./backend
    restart: always
    ports:
      - "8686:8686"
    command: uvicorn api.main:app --host 0.0.0.0 --port 8686

  frontend:
    build: ./frontend
    restart: always
    ports:
      - "8080:80" # ホストの8080をコンテナの80にマッピング